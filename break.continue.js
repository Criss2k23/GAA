let c = 0, mensaje = '';

while (c < 10){
    c++
    if (c === 50){
        //document.write("termina el bucle del 'BREAK' ")
        //break;
        document.write("Salta a la siguiente linea por 'CONTINUE' " + '<br>')
    }
    mensaje = `${c} <br>`
    document = `${c} <br>`
    document.write(mensaje);
}
